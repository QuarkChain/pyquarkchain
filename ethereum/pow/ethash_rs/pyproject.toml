[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "ethash-rs"
requires-python = ">=3.13"

[tool.maturin]
# Install the extension into ethereum/pow/ package namespace
module-name = "ethereum.pow.ethash_rs"
features = ["pyo3/extension-module"]
